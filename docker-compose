version: '3.8'

services:
  whatsapp-bot:
    build: .
    container_name: wa_imperator
    restart: unless-stopped
    environment:
      - BOT_TOKEN=ТВОЙ_ТОКЕН_СЮДА
      - ADMIN_ID=ТВОЙ_TELEGRAM_ID_СЮДА
    volumes:
      - ./sessions:/app/sessions
      - ./bot_database.db:/app/bot_database.db
    shm_size: '512mb'
